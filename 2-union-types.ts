// Declara una variable que puede ser de tipo string o número
let statusCode: string | number;

// Asigna diferentes valores para probar ambos tipos
statusCode = 200;
console.log(statusCode);

statusCode = "OK";
console.log(statusCode);
